<!DOCTYPE HTML>
<html>
  <head>
    <meta charset="UTF-8">
    <title>{{it.title}}</title>
    <style type="text/css">
        body {
          background: #000000;
          margin: 8px;
          color: #FFB000;
          font: normal 16px/20px Verdana, Tahoma, sans-serif;
          text-align: center;
          line-height: 24px;
        }

        hr {
          height: 1px;
          background-color: #FFB000;
          border: none;
        }

        hr.half {
          width: 50%;
        }

        article {
          display: inline-block;
          margin-left: 20px;
          width: 10%;
        }

        article:first-child, article:last-child {
          padding: 10px;
          background-color: #232B2B;
          width: 33%;
        }
    </style>
  </head>
  <body>
    <header>
      <h1>
        {{it.compobj.name}}
        {{@if(it.competition.Continent)}}
        {{it.competition.Continent.name}}
        {{/if}}
      </h1>
      <hr class="half" />
      <h2>{{it.hostname_suffix}}</h2>
    </header>

    <section>
      <article>
        <h3>{{it.team1.name}}</h3>
        <h4>{{it.team1_subtitle}}</h4>
      </article>
      <article>
        <h3>vs</h3>
      </article>
      <article>
        <h3>{{it.team2.name}}</h3>
        <h4>{{it.team2_subtitle}}</h4>
      </article>
    </section>
  </body>
</html>
